BoxLayout:
    orientation: 'vertical'

    Button:
        text: 'show_MDDialog'
        on_release:
            app.show_MDDialog()

    Button:
        text: 'show_MDInputDialog'
        on_release:
            app.show_MDInputDialog()
